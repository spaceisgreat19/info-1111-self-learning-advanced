generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client" // Adjust path as needed
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Vote {
  id     Int    @id @default(autoincrement())
  option String
  userId Int
}
